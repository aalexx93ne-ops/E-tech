def search_query(request):
    """
    Добавляет переменную query в контекст всех шаблонов.
    Это нужно для отображения текущего поискового запроса в меню.
    """
    return {
        'query': request.GET.get('q', '')
    }
